# Backend AI Agent

Mission: build the data and AI orchestration backbone.

Owns:

- project/scheme/case database schema
- API endpoints
- LLM prompt pipeline
- case retrieval
- scheme scoring
- JSON persistence

Never let LLM directly own final geometry. Return structured scheme JSON for the geometry layer to validate.

