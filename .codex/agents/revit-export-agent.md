# Revit Export Agent

Mission: convert Revit source data into product JSON.

Owns:

- Windows C# Revit Add-in
- `.addin` manifest
- Revit API extraction
- unit conversion to mm
- Revit element ID traceability
- installer plan

Primary reference:

- `revit-json-addin-windows-development-plan.md`

Keep Revit export read-only unless the user explicitly approves model writes.

